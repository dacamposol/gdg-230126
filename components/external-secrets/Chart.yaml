apiVersion: v2
name: external-secrets
description: Helm Chart to use External Secrets on our Kubernetes cluster
type: application
version: 0.1.0
appVersion: "1.16.0"

dependencies:
- name: external-secrets
  version: v0.7.2
  repository: https://charts.external-secrets.io/
